# 트레이 내 OCV 구배 원인 분석

> 트레이 단위 OCV 측정에서 관찰되는 셀 위치 의존적 전압 구배(gradient)의 원인 규명.
> 전압에 영향을 미치는 인자를 전수 나열하고, 실측된 구배의 특징(크기·패턴·간헐성)과
> 대조하여 유력 원인을 좁힌다.

---

## 1. 문제 정의

- 트레이(12행 × N열) 단위로 셀 OCV를 측정하면, 셀의 **트레이 내 위치**에 따라
  ΔOCV(셀 OCV − 트레이 중앙값)가 공간적 구조를 갖는 현상이 관찰된다.
- 구배 진폭은 판정 오프셋(0.8 mV)의 20~40% 수준으로, 자기방전 불량 판정의
  마진을 잠식할 수 있어 원인 규명이 필요하다.

---

## 2. 구배의 특징 (실측 기반)

| 항목 | 실측값 | 의미 |
|---|---|---|
| 구배 진폭(트레이 내 peak-to-peak) | 0.16~0.31 mV | 판정 오프셋 0.8 mV의 20~40% — 무시 못 하지만 지배적이지 않음 |
| [27b] 갤러리 스케일(중앙값 제거 후) | ±0.093 mV | 대다수 셀의 위치 효과는 ±0.1 mV 이내 |
| 공통 지문(전 트레이 평균 필드) | ±0.03 mV | 미세 — "모든 트레이가 공유하는 강한 지문"은 없음 |
| 트레이 간 필드 상관 | 중앙값 0.44, 넓게 퍼짐 | 패턴이 **간헐적** — 강한 트레이와 무작위 트레이 혼재 |

### 2.2 [27b] 갤러리에서 눈으로 확인되는 패턴 유형

100개 트레이 필드를 보면 다음 유형이 반복된다 (같은 랏 안에서 공존):

1. **링형 (테두리 高 / 중앙 低)** — 가장 흔한 유형. 테두리 셀들의 ΔOCV가 붉고(크고) 중앙 블록이 파랗다(작다). 1페이지 1행, 2행 좌측 트레이 등에서 뚜렷.
2. **역링형 (중앙 高)** — 링형의 반대. 3페이지 3행 좌측처럼 중앙에 붉은 덩어리.
3. **일방향 구배** — 위/아래/좌/우 한쪽으로 쏠림. 특히 **아래쪽 테두리(12행)가 붉은 트레이가 자주 보임**.
4. **대각선/사선 밴드** — 드물지만 존재. 3페이지 하단의 강한 파란 사선 밴드, 4페이지 2행 좌측의 X자형.
5. **세로 줄무늬** — 3페이지 하단 우측 등. 열(column) 단위로 색이 갈림.
6. **고립 핫셀** — 트레이당 1~4개의 진한 붉은/파란 단일 셀. (N32S D29 랏의 "불량핀 2.84%, 트레이당 ~4개"와 크기가 일치)
7. **무작위** — 상당수 트레이는 구조 없음.
8. **결측 밴드** — 특정 CELL NO 행이 흰색(저표본 밴드, 원인 미해결).

---

## 3. 전압(OCV 측정값)에 영향을 미치는 인자 전수 목록

측정된 OCV = **셀 진짜 OCV** + **환경(온도) 효과** + **측정계 오차** 로 분해된다.
각 성분별 인자를 전부 나열하고, 예상 크기와 "트레이 내 위치 패턴을 만들 수 있는가"를 표기한다.

### 3.1 셀 진성(intrinsic) 인자 — 셀마다 다르고 위치와 무관하게 섞여 들어옴

| # | 인자 | 메커니즘 | 예상 크기 | 위치 패턴 생성? |
|---|---|---|---|---|
| A1 | SOC 편차 | 포메이션/충전 채널별 충전량 차이 → OCV-SOC 곡선 기울기(수 mV/%SOC)를 타고 전압차 | 수백 µV ~ 수 mV | 채널→위치 매핑이 고정이면 **예** (줄무늬) |
| A2 | 자기방전율 편차 | 미세단락·금속 이물·SEI 품질 차이 → 시간당 전압강하 차이 | 정상 수 µV/day, 불량 수십~수백 µV/day | 아니오 (무작위) → **고립 핫셀** |
| A3 | 전압 이완(relaxation) 상태 | 충전 종료 후 OCV는 수 시간~수 일에 걸쳐 안정화. 충전 종료 시각이 셀마다 다르면 이완 정도가 다름 | 초기 수 mV/h → 수십 µV/h로 감쇠 | 충전 순서가 위치에 매핑되면 **예** |
| A4 | 음/양극 용량 밸런스, 전해액 함침 편차 | 동일 SOC에서도 국부 평형전위 차이 | 수십~수백 µV | 아니오 (제조 무작위) |
| A5 | 애노드 히스테리시스 | 직전 충/방전 방향에 따라 OCV가 다름 (흑연 stage 전이) | 수 mV까지 가능하나 랏 내 동일 이력이면 상쇄 | 아니오 |

### 3.2 환경(온도) 인자 — **위치 패턴을 만드는 가장 자연스러운 경로**

OCV는 온도의 함수다: **dOCV/dT (엔트로피 계수)는 SOC에 따라 수십~수백 µV/K**
(리튬이온 셀에서 통상 −0.3 ~ +0.3 mV/K, 30~50% SOC 부근에서 절대값 0.05~0.2 mV/K가 흔함).
→ **트레이 내 1~2 °C 온도 구배만으로 0.1~0.3 mV의 OCV 구배가 생긴다.
이는 실측 진폭(0.16~0.31 mV)과 정확히 같은 자릿수다.**

| # | 인자 | 메커니즘 | 만들어지는 패턴 |
|---|---|---|---|
| B1 | 열평형 미도달(측정실 이동 직후) | 에이징 챔버(고온 또는 저온)에서 측정 환경으로 이동하면 **테두리 셀부터 식거나 데워짐**. 중앙 셀은 이웃 셀에 둘러싸여 열 시상수가 김 | **링형 / 역링형** (이동 방향·대기시간에 따라 부호 반전) |
| B2 | 챔버 내 공조 기류·성층 | 송풍 방향, 선반 높이, 수직 온도 성층(바닥이 차가움) | **일방향 구배** (특히 상/하 방향, "12행이 붉은" 패턴) |
| B3 | 트레이 적층/이웃 트레이 | 위·아래 트레이의 복사·전도, 벽면 인접 여부 | 일방향 또는 비대칭 링형 |
| B4 | 셀 자기발열 잔열 | 충전 직후 셀 내부 발열이 중앙에서 더 늦게 빠짐 | 링형 계열 |
| B5 | 측정 중 국부 기류 (문 개방, 작업자, 조명) | 순간적·불규칙 온도 요동 | **간헐적** 무작위/사선 |

> **핵심**: 온도 경로는 트레이마다 열 이력(챔버 내 위치, 반출 후 대기시간)이 달라
> 패턴의 **부호·강도가 트레이마다 다르게** 나타난다 → 실측된 "간헐성"(상관 중앙값 0.44,
> 강한 트레이와 무작위 트레이 혼재)과 부합한다.

### 3.3 측정 시스템 인자

| # | 인자 | 메커니즘 | 예상 크기 | 위치 패턴 생성? |
|---|---|---|---|---|
| C1 | 채널별 오프셋(멀티플렉서/ADC 뱅크) | 채널 캘리브레이션 잔차 | 수십 µV | **예 — 그러나 전 트레이 공통 고정 지문**이어야 함 |
| C2 | 프로브 접촉 (핀 오염·마모·가압 편차) | 접촉저항 × 계측기 입력 바이어스 전류. 전압계 입력임피던스가 충분히 크면 미세 | 정상 수 µV, 불량 핀은 수백 µV~ | 예 (특정 핀 위치 고정) → **고립 핫셀·결측 밴드** |
| C3 | 열기전력(thermal EMF) | 이종 금속 접점(핀-배선-커넥터)의 온도차, 수 µV/K | 수~수십 µV | 지그 내 온도구배가 있으면 예 |
| C4 | 스캔 순서 × 시간 드리프트 | 셀을 순차 측정하는 동안 셀 온도/이완이 변하면 측정 순서가 공간 패턴으로 각인 | 스캔 시간 × 드리프트율 | **예 — 사선/세로 줄무늬** (스캔 경로 모양) |
| C5 | 계측기 웜업/드리프트, 기준전압 온도계수 | 측정기 자체 오프셋의 시간 변화 | 수십 µV/h | 트레이 간 오프셋 (트레이 내 구배는 약함) |
| C6 | 배선 길이·누설전류 차이 | 채널별 배선 경로 차이 | 수 µV | 예 (고정 지문) |

### 3.4 공정 이력 인자

| # | 인자 | 메커니즘 | 위치 패턴 생성? |
|---|---|---|---|
| D1 | 포메이션 장비 채널 → 트레이 위치 매핑 | 채널별 충전 전압/전류 캘리브레이션 오차가 위치에 고정 매핑되면 SOC 편차가 공간 패턴이 됨 | **예 — 세로 줄무늬**의 유력 후보 |
| D2 | 충전 종료 시각의 위치 의존 | 채널 그룹별 순차 종료 → 이완 시간차 | 예 (블록/줄무늬) |
| D3 | 디개싱·프레스 등 후공정의 처리 순서 | 처리 시간차 → 이완·온도 이력차 | 예 (처리 경로 모양) |
| D4 | 트레이 재적재(re-tray) 시 셀 재배열 | 이전 공정의 위치 패턴이 섞이거나 이월됨 | 패턴 희석 또는 전사 |

---

## 4. 실측 특징과 인자별 대조 — 원인 좁히기

### 4.1 판별 논리

실측이 주는 강한 제약 두 가지:

1. **공통 지문이 ±0.03 mV로 미세하다.**
   → 측정 지그·채널 오프셋(C1, C6)처럼 **모든 트레이에 똑같이 찍히는 고정 오프셋은
   주범이 아니다.** 고정 오프셋이 주범이라면 전 트레이 평균 필드에 강하게 남아야 한다.
2. **패턴이 간헐적이다** (트레이 간 상관 중앙값 0.44, 강한 트레이와 무작위 트레이 혼재,
   링형·역링형·일방향이 같은 랏에서 공존).
   → 원인은 **트레이마다 달라지는 변수**, 즉 **열 이력**(챔버 내 위치, 반출 후 경과시간,
   기류)이거나 **처리 타이밍**이다. 링형↔역링형의 부호 반전은 "냉각 국면이냐 가열
   국면이냐"로 자연스럽게 설명된다.

### 4.2 정량 일치성 (온도 가설)

```
필요 온도차 = 구배 진폭 / |dOCV/dT|
            = 0.16~0.31 mV / (0.1~0.2 mV/K)
            ≈ 1~3 °C
```

트레이 테두리와 중앙의 1~3 °C 차이는 챔버 반출 직후 수십 분 동안 흔히 발생하는
크기다(셀 열 시상수 30분~수 시간). **온도 가설은 크기·패턴·간헐성 세 가지를 모두
동시에 설명하는 유일한 단일 인자다.**

### 4.3 패턴 유형별 유력 원인 매핑

| 패턴 | 1순위 원인 | 2순위 원인 | 판별 포인트 |
|---|---|---|---|
| 1. 링형 (테두리 高) | **B1 열평형 미도달** (테두리가 측정 환경 온도에 먼저 수렴) | B4 자기발열 잔열 | 반출→측정 대기시간과 진폭의 상관 |
| 2. 역링형 (중앙 高) | **B1의 부호 반전** (가열 국면 또는 dOCV/dT 부호가 다른 SOC) | — | 링형 트레이와 열 이력 비교 |
| 3. 일방향 (12행 붉음) | **B2 수직 성층/기류** 또는 B3 이웃 트레이 | C4 스캔 순서 | 트레이 180° 회전 재측정 시 패턴이 뒤집히는지 |
| 4. 대각선/사선 밴드 | **C4 스캔 순서 × 드리프트** (스캔 경로가 사선이면) | B5 국부 기류 | 스캔 순서 번호 vs ΔOCV 상관 |
| 5. 세로 줄무늬 | **D1 포메이션 채널 매핑** | C1 측정 MUX 뱅크 | 포메이션 채널 ID vs ΔOCV; 채널이면 랏 불문 재현 |
| 6. 고립 핫셀 | **A2 셀 자기방전 이상** (불량핀 2.84%, 트레이당 ~4개와 크기 일치) | C2 접촉 불량 핀 | 재측정 시 같은 셀이 따라가는지(셀) / 같은 위치에 남는지(핀) |
| 7. 무작위 | 셀 개별 편차(A1~A4)가 지배, 열 구배 미미 | — | 정상 상태로 간주 |
| 8. 결측 밴드 | **측정/데이터 시스템** (특정 행 채널 결측·판독 실패) | 데이터 파이프라인 파싱 | 원시 로그에서 해당 행 채널 상태 확인 |

### 4.4 결론 — 원인 우선순위

1. **트레이 내 온도 구배 (B1, B2)** — 주범. 링형·역링형·일방향 패턴, 0.1~0.3 mV
   진폭, 간헐성 모두 설명. 챔버 반출 후 열평형 미도달 상태에서 측정하는 것이 핵심 경로.
2. **포메이션 채널 → 위치 매핑 (D1)** — 세로 줄무늬의 유력 원인. 고정 매핑인데도
   공통 지문이 약한 이유는 채널 캘리브레이션이 랏/시점마다 다르기 때문일 수 있음.
3. **스캔 순서 × 시간 드리프트 (C4)** — 사선 밴드의 후보.
4. **셀 자기방전 이상 (A2) / 접촉 불량 (C2)** — 고립 핫셀. 구배가 아닌 점 결함으로,
   기존 불량핀 검출과 일치.
5. **측정계 고정 오프셋 (C1, C6)** — 공통 지문 ±0.03 mV 이내로 **기각에 가까움**
   (기여하더라도 부차적).

---

## 5. 원인 확정을 위한 검증 실험 제안

| # | 실험 | 방법 | 판별 |
|---|---|---|---|
| V1 | **트레이 180° 회전 재측정** | 동일 트레이를 즉시 회전 후 재측정 | 패턴이 셀을 따라가면 → 셀/열 이력(B·D), 위치에 남으면 → 측정계(C) |
| V2 | **대기시간 스윕** | 챔버 반출 후 10분/30분/1시간/3시간 시점별 측정 | 진폭이 시간에 따라 감쇠·부호반전하면 → 온도(B1) 확정 |
| V3 | **트레이 내 다점 온도 로깅** | 테두리/중앙 셀에 열전대 부착, 반출→측정까지 기록 | ΔT 필드와 ΔOCV 필드의 공간 상관 확인 |
| V4 | **dOCV/dT 실측** | 해당 SOC에서 셀 몇 개를 항온조 온도 스텝 | 필요 온도차 계산의 계수 확정 |
| V5 | **포메이션 채널 ID 대조** | 채널 ID를 트레이 좌표에 매핑해 ΔOCV와 상관 분석 | 세로 줄무늬가 채널 경계와 일치하면 D1 확정 |
| V6 | **스캔 순서 대조** | 측정 타임스탬프(순서)와 ΔOCV의 상관; 스캔 방향을 바꿔 재측정 | 사선 밴드가 스캔 경로를 따라가면 C4 확정 |
| V7 | **핫셀 추적** | 핫셀을 다른 위치/트레이로 옮겨 재측정 | 셀을 따라가면 A2(자기방전), 위치에 남으면 C2(핀) |
| V8 | **결측 밴드 원시 로그 확인** | 해당 CELL NO 행의 측정기 원시 데이터/에러 코드 확인 | 채널 하드웨어 vs 데이터 파이프라인 구분 |

### 5.1 개선 방향 (원인 확정 시)

- **온도(B)**: 반출 후 안정화 시간 표준화(예: 열 시상수의 3배), 측정실 온도 균일화,
  또는 트레이 위치별 온도 보정 계수 적용.
- **채널(D1)**: 포메이션 채널 캘리브레이션 주기 단축, 채널→트레이 매핑을 주기적으로
  셔플하여 위치 고착 방지.
- **스캔(C4)**: 스캔 시간 단축 또는 스캔 순서 무작위화, 타임스탬프 기반 드리프트 보정.
- **판정 로직**: 트레이 내 공간 저주파 성분(구배)을 제거한 잔차 기반 판정으로 전환하면
  구배 0.16~0.31 mV가 판정 마진(0.8 mV)을 잠식하는 문제를 원천 차단.

---

## 6. 요약

- 전압에 영향을 주는 인자는 **셀 진성(A) / 온도·환경(B) / 측정계(C) / 공정 이력(D)**
  4개 군, 총 20개로 정리된다.
- 실측된 두 가지 제약 — **약한 공통 지문**(고정 오프셋 기각)과 **간헐적 패턴**(트레이별
  가변 원인) — 을 동시에 만족하는 주범은 **트레이 내 온도 구배(열평형 미도달 + 챔버
  기류/성층)** 이다. 필요 온도차 1~3 °C는 물리적으로 흔한 크기다.
- 세로 줄무늬는 포메이션 채널 매핑, 사선은 스캔 순서, 고립 핫셀은 셀 자기방전/접촉
  불량으로 각각 분리 설명된다.
- 확정은 V1(회전)·V2(대기시간)·V3(온도 로깅) 3개 실험으로 가능하며, 판정 로직을
  구배 제거 잔차 기반으로 바꾸면 실무 영향은 즉시 완화된다.
