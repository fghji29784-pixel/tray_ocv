# 구배 발생 지점 수학적 규명 + 발표용 시각자료 — 구현 계획

> 목적: docv7 링 구배가 **어느 공정에서 태어나는지**를 눈 판단이 아니라 수학으로
> 확정하고, 발표에 쓸 **단계별 시각자료**를 풍부하게 생성한다.

---

## A. 수학 방법 (신규 모듈 `analysis/genesis_math.py`)

각 트레이에서 단계 k의 ΔOCV 필드를 벡터 fₖ∈ℝ¹⁴⁴(트레이 중앙값/mode 제거, 12×12
펼침)로, 최종 판정 구배를 d=Δdocv7 로 둔다. 공통 유효셀에 정렬해 비교.

1. **누적 정렬도 aₖ** = corr(fₖ, d) — "이 단계에 이미 최종 패턴이 있나". 단계별
   트레이 분포(중앙값·부호일관성).
2. **증분 투영 βₖ** = ⟨gₖ, d⟩ / ‖d‖², gₖ = fₖ − fₖ₋₁ — "이 공정이 패턴을 새로
   주입했나". **핵심 원인 지목**. matched-SOC / composes_docv7 플래그 유지.
3. **Moran's I** Iₖ — 단계별 공간 자기상관(이웃 유사도). rook 인접 가중, E[I]=−1/(n−1)
   대비 + (옵션)순열 p값. "패턴 종류 불문 공간 구조의 탄생" 검정.
4. **변화점 k\*** — aₖ 수열에 CUSUM/이분탐색 → 계단 상승 단계를 통계적으로 1개 지목.

**종합 판정**: aₖ 급등 단계 = 존재 시작, βₖ 최대 공정 = 생성 공정, Iₖ 최초 유의 =
구조 발생, k\* = 통계 확정. 넷이 같은 단계면 원인 확정.

산출 표: `genesis_alignment.csv`(aₖ,Iₖ,p,k\*), `genesis_beta.csv`(공정별 βₖ).

## B. 발표용 시각자료 (`analysis/viz.py` 확장 → `reports/figures/`)

단계별로 넘겨볼 수 있게, 일관된 스타일(공유 컬러스케일·영문 라벨·고DPI).

1. **단계별 OCV 갤러리** — OCV1~7·전용OCV 각각, 같은 트레이 셋·같은 스케일.
   순서대로 넘기면 링이 "켜지는" 순간이 보임. (`gallery_stage_<label>.png`)
2. **공정 증분 필드 갤러리** — gₖ(공정 동안의 변화) 필드, matched-SOC 강조.
   "이 공정이 무엇을 더했나".
3. **스토리보드(핵심 발표컷)** — 대표 트레이 몇 개를, 전 단계를 가로로 나열한
   small-multiples 1장. 한 트레이의 링이 어느 칸에서 나타나는지 스토리로 보임.
4. **βₖ 막대그래프** — 공정별 기여(원인 슬라이드). matched-SOC 색 강조, composes_docv7 제외 표시.
5. **aₖ + Iₖ 진행 곡선** — 공정 순서 x축, 변화점 k\* 세로선 표시.
6. **링 진폭 진행** — 기존 유지(단계별 링 크기).
7. **공통 지문 필드** — 전 트레이 평균 필드(단계별): 모든 트레이 공유 패턴 유무.
8. **패턴 유형 갤러리** — 트레이를 링/역링/일방향/무작위로 군집화해 대표 예시.
   (초기 관찰의 8유형과 연결)

## C. 파이프라인/리포트 반영

- `run_pipeline` S8 확장: genesis_math 호출 → 표·그림 저장.
- `report_run.md`: aₖ/βₖ/Iₖ/k\* 표 추가, 종합 판정 문장 자동 생성
  ("구배는 <공정>에서 발생: a 급등 <단계>, β 최대 <공정>, I 최초유의 <단계>").
- 그림은 `reports/figures/` (발표용 `presentation/` 하위 정리).
- 큐레이션: 갤러리 트레이는 링진폭 상위 N + 무작위 N (config `gallery_n`).

## D. 리스크·파라미터

- **계산량**(149k셀×1035트레이): β·a는 트레이별 선형대수(빠름). Moran's I 순열은
  비용↑ → 기본 analytic z + 순열은 트레이 서브샘플·소수 옵션.
- **공통 유효셀 정렬**: 단계마다 결측 위치가 달라 <fₖ,d> 비교 시 공통셀만 사용.
- **중심통계**: d(docv7)는 mode 중심(판정 일관), 온도/OCV 필드는 옵션.
- **갤러리 트레이 수**: 1035개 전부는 불가 → 큐레이션(상위+무작위).
- **폰트**: 그림 라벨 영문(한글 폰트 미설치 대응).

## E. 구현 순서

| 순서 | 항목 |
|---|---|
| 1 | genesis_math: a, β, Moran's I, changepoint + 표 |
| 2 | 단계별 OCV 갤러리 + 스토리보드 |
| 3 | βₖ 막대 + aₖ/Iₖ 곡선(변화점) |
| 4 | 공정 증분 갤러리 + 공통지문 + 패턴유형 갤러리 |
| 5 | run_pipeline 통합 + 리포트 종합판정 + 테스트 |
