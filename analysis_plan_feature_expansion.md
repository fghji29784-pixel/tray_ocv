# 온도 피처 확장 · OCV 필드 상관 심화 — 계획

> 요청 반영: ① min/mean/max 각각을 OCV 필드와 직접 비교 ② 온도-보유 스텝 간
> 편차로 사이 공정의 열영향 추론 ③ 연속 공정의 열 궤적 피처(C5최저=시작,
> C7최고=중간 등) ④ 중심통계 median→mode 재검토 ⑤ 칼럼당 인자 추가 발굴.

---

## 0. 데이터 현실 (먼저 확정)

**온도 보유 칼럼**: Low Current Inspection #01/#02(값 1개), Charge #01~07(최저/평균/
최고), DisCharge #01~07(최저/평균/최고), PRIVT OCV #01~03(값 1개).
**온도 없음**: 중간 OCV #01~#07 (OCV 값만). → "OCV 온도 편차"는 온도가 있는
스텝(충·방전·전용OCV·LCI) 사이의 편차로 해석한다.

확정된 공정 순서 (연속성 판단의 근거):
```
LCI>RT1>OCV1>C1>C2>OCV2>HT1>RT2>OCV3>C3>C4>OCV4>HT2>RT3>OCV5>
C5>C6>C7>OCV6>D1>D2>D3>D4>D5>D6>D7>OCV7>RT4>PRVT1>RT5>PRVT2>RT6>PRVT3
```
- **연속(쉬지 않음) 충전 블록**: {C1,C2} / {C3,C4} / {C5,C6,C7}
- **연속 방전 블록**: {D1…D7}
- 블록 사이엔 OCV 측정·에이징(휴지)이 있어 냉각 → 블록 경계에서 온도 점프.

---

## 1. 스텝별 3통계 개별 활용 (요청 ①)

현재는 평균만 대표로 쓰고 (최고−최저)만 파생. 개선:

- `ΔT_min::step`, `ΔT_mean::step`, `ΔT_max::step` 를 **각각 별도 예측자**로 노출
  (트레이 중앙값 제거 필드). 전부 S5 스크리닝에 투입.
- 파생:
  - `rise = max − min` (스텝 내 자기발열 폭)
  - `skew = (max − mean) − (mean − min)` (승온 비대칭: 초반 급상승 vs 후반 급상승)
- 각 통계가 **어느 OCV 단계 필드**와 닮았는지 따로 본다 (아래 §4 타깃 확장).

## 2. 연속 블록 열 궤적 피처 (요청 ③ — C5최저=시작, C7최고=중간 유추)

연속 충/방전 블록은 자기발열로 온도가 단조 상승한다는 물리를 이용해 **블록 단위
열 이력**을 복원한다. 블록 B = 연속 스텝들(예 {C5,C6,C7}):

| 피처 | 정의 | 물리 의미 |
|---|---|---|
| `block_entry` | 첫 스텝의 최저 (예 C5.min) | 블록 시작 온도(직전 냉각 상태) |
| `block_peak` | 블록 전체 최고 (예 C7.max 부근) | 자기발열 피크(공정 중후반) |
| `block_rise` | peak − entry | 블록 총 발열량(위치별 열저항·발열 차이) |
| `block_rate` | rise / (스텝수) | 단위 스텝당 승온율 |
| `block_mean` | 스텝 평균들의 평균 | 블록 평균 노출 |
| `block_area` | Σ(스텝 평균) | 누적 열 노출(간이 적분) |

- 각 블록마다 위 피처의 **ΔT필드**(트레이 중앙값 제거) 생성 → 예측자.
- **블록 진입온도(entry)** 는 특히 중요: 직전 에이징(냉각) 상태를 반영 → 관측
  안 되는 에이징 챔버 구배의 **간접 프록시**(테두리가 더 식었나/덜 식었나).

## 3. 온도-보유 스텝 간 편차 = 사이 공정 열영향 (요청 ②)

인접한 두 온도 측정점의 차이로, 그 사이 공정이 **위치별로** 온도에 어떤 영향을
줬는지 필드로 만든다:

- `Δbetween(a→b) = T_b − T_a` (같은 통계끼리; 예 C7.max − C5.min, D1.min − C7.max)
- 핵심 구간:
  - `C7.max − C5.min` : {C5C6C7} 충전블록의 총 발열 (위치별)
  - `D1.min − C7.max` : 만충 후 OCV6·방전진입까지의 **냉각량** (사이에 OCV6 휴지)
  - `D7.max − D1.min` : 방전블록 발열
  - `PRVT01.온도 − D7.mean` : 방전 후~전용OCV까지 냉각/이력
- **HT 에이징(온도 없음) 브라케팅**: HT1 직전 마지막 온도(C2.max)와 직후 첫 온도
  (C3.min) 의 차이 = HT1+RT2 동안의 냉각/열이력 프록시. HT2도 동일(C4.max↔C5.min).
  → 온도가 없는 고온에이징의 위치별 영향을 **인접 충전 온도로 간접 추정**.

## 4. 타깃(OCV 필드) 확장

현재 상관 타깃: `d_ocv1/2/3`, `d_docv7`.
확장: **모든 OCV 단계 필드** `d_ocvstage::OCV#01…#07`, `PRVT#01…#03` 추가.

- 각 온도/궤적 피처가 **어느 OCV 단계 구배와 닮았는지** 매트릭스로.
- 이러면 §S8(구배 발생 추적)과 연결: "Charge#05 승온 필드 ↔ OCV#06 링" 처럼
  공정 온도와 그 직후 OCV 구배의 대응을 직접 확인 → 원인 국소화 강화.

## 5. 중심통계: median vs **mode** (요청 ④)

현재 트레이내 편차는 `ΔX = X − median_tray(X)`. 그런데 **판정은 mode**를 쓴다
(`docv7_dev = docv7 − tray_mode`). 불일치 존재.

- 계획: 중심통계를 **옵션화**(`center_stat: median | mode | mean | trimmed`).
- 근거:
  - **docv7 필드는 mode 중심이 타당** — 판정과 동일 기준(정상 셀 다수의 값)을
    빼야 "판정에서 벗어나는 셀"이 그대로 구배로 드러남. → docv7 계열은 mode 기본.
  - 온도 필드는 연속·매끄러움 → median/mean 무난하나 mode도 옵션 제공.
  - mode는 연속값이라 히스토그램 bin 필요(이미 `judge.mode_bin_mv`, 온도용 bin 별도).
- **민감도 리포트**: center_stat별 링 진폭·상관·판정 뒤집힘 비교표 자동 산출.

## 6. 칼럼당 추가 인자 발굴 (요청 ⑤ 확장)

온도 외에 각 스텝 칼럼에서 뽑을 수 있는 것:

- **작업시간(시작/종료/작업시간)**: 스텝 소요시간 → 열 노출 시간·라인 정체 지표.
  - `Σ heat = Σ (mean_temp 관련) × duration` 아레니우스 가중(자기방전·SEI).
  - 측정 순서/타임스탬프 → 스캔 순서 효과(사선 밴드) 점검.
- **중간 OCV 값 자체(온도 아님)**: 이미 S8에서 사용. 여기에 단계 간 차(자기방전
  구간)도 예측자화.
- **등급/Lot/공정경로**: 층화·교란 통제 변수(트레이 간 원인 분리).
- 전 스텝 온도의 **트레이 평균**(절대수준): 트레이가 통째로 뜨거운지 — 트레이
  간 차이용 참고(트레이 내 구배 원인은 아니지만 상호작용 점검).

## 7. 분석 파이프라인 반영

- **S5 스크리닝**: 확장 예측자(수십→수백) × 확장 타깃(OCV 전 단계). 조합 폭증 →
  기존 BH-FDR·permutation 유지 + **예측자 군집화(상관 기반)** 로 대표만 표에 노출.
- **S6 회귀**: min/mean/max·블록 피처는 강한 공선성 → 대표 1개 또는 **직교화(PCA)**
  후 투입. VIF 점검.
- **S8 발생추적**: 온도피처↔OCV단계 상관을 발생추적 표에 병합(공정별 기여에
  "온도로 설명되는 몫" 열 추가).

## 8. 주의 / 리스크

- **공선성**: min/mean/max, 블록 피처는 서로 매우 닮음 → 개별 상관(S5)엔 다 넣되
  회귀·보정엔 대표/직교화. 안 그러면 계수 폭주(이미 겪은 Ea 3종 문제와 동일).
- **계산량**: 149k셀×1035트레이. 예측자 수백 개 × OCV 단계 10+ 타깃 → permutation
  비용 급증. 벡터화 유지 + 1차 스크리닝은 permutation 축소(예 100) 후 상위만 정밀.
- **치환 부풀림**: 3×3 치환이 공간 상관 과대평가 → 확장 피처도 치환 포함/제외
  두 벌 산출 유지.
- **해석 함정**: docv7을 산술 구성하는 구간(RT5/RT6=PRVT1→3)은 자명한 상관 → 이미
  `composes_docv7` 로 제외. 신규 피처도 동일 원칙.

## 9. 구현 순서(제안)

| 순서 | 항목 | 산출 |
|---|---|---|
| 1 | 중심통계 옵션화(median/mode/…) + 민감도표 | §5 |
| 2 | min/mean/max 개별 ΔT필드 + skew/rise | §1 |
| 3 | 타깃에 OCV 전 단계 필드 추가 | §4 |
| 4 | 연속 블록 열궤적 피처 | §2 |
| 5 | 스텝 간 편차(사이 공정·HT 브라케팅) | §3 |
| 6 | 작업시간·기타 칼럼 인자 | §6 |
| 7 | 회귀 직교화 + S8 통합 리포트 | §7 |

→ 1~3만 먼저 해도 "각 통계·전 단계 상관"과 mode 이슈가 해결됨. 4~6은 심화.
