# 공정 단계별 온도 데이터 기반 OCV 구배 발생·전파 추적 — 분석 계획

> 목표: 공정 체인 어느 단계에서 트레이 내 구배가 **만들어지고**, 그것이 어떻게
> **이어져(전파되어)** 최종 판정값 dOCV7의 트레이 내 구배가 되는지를
> 온도 데이터로 규명한다.

---

## 0. 배경 정리

### 0.1 공정 체인

```
미세충전 → 충전 → 에이징 → 고온에이징(60°C) → 에이징 → 충전 → 고온에이징
 → 에이징 → 충전 → 방전 → 에이징 1일 → [전용OCV1] → 에이징 2일 → [전용OCV2]
 → 에이징 1일 → [전용OCV3]
```

### 0.2 판정 로직

- dOCV7 = 전용OCV1 − 전용OCV3 구간의 전압 변화량 (셀별)
- 판정: `(셀 dOCV7 − 트레이 dOCV7 mode) > 0.8 mV → 불량`
- 따라서 문제가 되는 것은 dOCV7의 **트레이 내 위치 의존 성분**
  (구배 진폭 0.16~0.31 mV는 판정 마진 0.8 mV의 20~40%를 잠식).

### 0.3 핵심 물리 — dOCV7은 차분이므로 구배 생성 경로가 3개로 좁혀진다

dOCV7 = OCV1 − OCV3 이므로, 두 측정 시점의 온도 필드가 동일하면 dOCV/dT 효과는
**상쇄**된다. 상쇄가 깨지는 경로는:

| 경로 | 메커니즘 | 온도 데이터에서 보이는 신호 |
|---|---|---|
| **P1. 측정 시점 온도차** | ΔT필드(OCV1 시점) ≠ ΔT필드(OCV3 시점) → dOCV/dT × [T₁(x,y) − T₃(x,y)] 가 그대로 구배로 | OCV1/OCV3 측정 시각의 트레이 내 온도 필드 차이 |
| **P2. 에이징 중 온도 의존 자기방전** | OCV1~OCV3 사이 4일간 따뜻한 위치의 셀이 더 많이 자기방전 (아레니우스: 10 °C↑ ≈ 자기방전 ~2배) | OCV1~OCV3 구간 에이징 중 시간평균 온도 필드 |
| **P3. 상류 공정 이월(carryover)** | 고온에이징(60 °C) 출입 시 테두리/중앙 열 이력 차이 → SEI 형성량·이완 상태 차이가 상태변수로 각인되어 dOCV7 구간까지 지속 | 60 °C 승온/강온 과도구간의 위치별 온도 적분(열 노출량) 차이 |

이 3개 경로를 분리 판별하는 것이 본 분석의 골격이다.

---

## 1. 단계 1 — 데이터 인벤토리 및 스키마 확인

> ⚠️ 현재 온도 데이터가 아직 리포/환경에 업로드되지 않음. **첫 작업은 데이터 업로드
> 후 스키마 파악.**

확인할 항목:

1. **온도 데이터의 분해능(granularity)** — 아래 중 어느 수준인가에 따라 분석 깊이가 달라짐:
   - (a) 셀별 온도 (최상) → 트레이 내 ΔT 필드 직접 재구성 가능
   - (b) 트레이 내 다점 센서 (테두리/중앙 등) → 필드 보간으로 근사
   - (c) 트레이당 1점 or 챔버 센서 → 트레이 내 구배 직접 관측 불가.
     대신 "챔버 내 트레이 위치 + 승온/강온 과도 모델"로 간접 추정 (분석 계획 B로 전환)
2. **시간 해상도** — 과도구간(승온/강온)을 볼 수 있는가 (분 단위 필요)
3. **조인 키** — 셀 ID / 트레이 ID / 트레이 내 좌표(행,열) / 공정 스텝 ID / 타임스탬프
4. **OCV 데이터와의 매칭** — 기존 전용OCV1~3 데이터와 셀 단위 조인 가능 여부
5. **커버리지** — 어떤 공정 스텝이 포함되어 있고 어떤 스텝이 빠져 있는가

산출물: `data/README.md`에 스키마 문서화 + 로딩/정합 스크립트.

## 2. 단계 2 — 공통 지표 정의 및 필드 재구성

온도와 전압을 **같은 좌표계의 필드**로 만들어 직접 비교 가능하게 한다.

1. 트레이 좌표계 통일: (행 1~12, 열 1~N)
2. 셀별·스텝별 온도 요약 통계:
   - `T_end(step)`: 스텝 종료 시점 온도
   - `T_mean(step)`: 스텝 중 시간평균 온도
   - `HeatExposure(step)`: 아레니우스 가중 열 노출량 ∫exp(−Ea/RT)dt (P2·P3용, Ea는 자기방전 활성화에너지 가정값 40~60 kJ/mol로 감도분석)
3. 트레이 내 편차 필드로 변환: `ΔX(cell) = X(cell) − median_tray(X)` — 트레이 간 차이를 제거하고 **트레이 내 구배만** 남김
4. 필드 구조 지표 (온도·전압 공통 적용):
   - 진폭: peak-to-peak, robust P95−P5
   - **링 점수**: mean(테두리) − mean(중앙)
   - **방향 구배**: 행/열 좌표에 대한 선형회귀 계수 (상하/좌우 쏠림)
   - 줄무늬 점수: 열별 평균의 분산
5. 전압 쪽 필드: ΔOCV1, ΔOCV2, ΔOCV3, ΔdOCV7 (기존 [27b] 갤러리와 동일 정의)

## 3. 단계 3 — 구배의 "발생 지점" 추적 (어디서 만들어지는가)

각 공정 스텝 k에 대해:

1. **스텝별 온도 구배 프로파일**: 트레이별 ΔT_k 필드의 진폭·링점수·방향구배를
   공정 순서대로 나열 → **어느 스텝에서 트레이 내 온도 구배가 가장 큰가**
   (예상: 60 °C 고온에이징 승온/강온 과도구간이 최대)
2. **과도 vs 정상상태 분리**: 각 스텝을 승온부/유지부/강온부로 쪼개 구배 진폭 비교
   → 구배는 항상 "온도가 변하는 순간" 테두리-중앙 시상수 차이로 생긴다는 가설 검증
3. **트레이 간 간헐성 재현 확인**: 스텝별 ΔT 필드의 트레이 간 상관 분포가
   기존 관측(ΔdOCV7 상관 중앙값 0.44, 간헐적)과 유사한 스텝 찾기

## 4. 단계 4 — 구배의 "전파" 추적 (어떻게 이어지는가)

### 4.1 경로별 판별 회귀

셀 단위, 트레이 고정효과 포함:

```
ΔdOCV7(cell) ~ β₁·[ΔT_OCV1시점 − ΔT_OCV3시점]     (P1: 측정 시점 온도차)
             + β₂·ΔHeatExposure(OCV1~OCV3 에이징)  (P2: 온도 의존 자기방전)
             + β₃·ΔHeatExposure(고온에이징 과도)     (P3: 상류 이월)
             + (트레이 랜덤효과) + ε
```

- 각 β의 유의성·기여 분산으로 **경로별 기여율 정량화**
- β₁의 물리적 타당성 교차검증: β₁ ≈ dOCV/dT (문헌/실측 0.1~0.2 mV/K)와 일치하는가
- 다중공선성 주의: 스텝 간 온도 필드가 서로 닮았으면 (테두리는 항상 테두리)
  경로 분리가 안 됨 → VIF 확인, 트레이별 열 이력이 다른 것을 이용해 분리

### 4.2 전파 사슬(chain) 분석

- 인접 스텝 간 필드 상관 행렬: corr(ΔT_k, ΔT_{k+1}), corr(ΔT_k, ΔOCV1),
  corr(ΔT_k, ΔdOCV7) → **스텝 × 타깃 상관 히트맵** 1장으로 "발생→전파→판정" 사슬 시각화
- ΔOCV1, ΔOCV2, ΔOCV3 각각과 스텝별 온도의 상관을 따로 보기:
  - P1이면: ΔOCV1과 ΔOCV3에 각각 해당 시점 온도가 찍히고 dOCV7에는 그 차이가 남음
  - P2면: ΔOCV1엔 약하고 ΔOCV3로 갈수록 누적 (OCV1→2→3 단조 증가 패턴)
  - P3이면: ΔOCV1 시점에 이미 구배가 완성되어 있고 OCV1~3 동안 거의 불변

### 4.3 패턴 유형별 층화 분석

기존 갤러리에서 분류한 8개 패턴 유형(링형/역링형/일방향/사선/줄무늬/핫셀/무작위/결측)별로
트레이를 나눠, 유형별로 온도 이력이 어떻게 다른지 비교:
- 링형 트레이 vs 무작위 트레이: 강온 과도구간 ΔT 진폭 차이가 있는가
- 링형 vs 역링형: 열 이력의 부호(냉각 국면 vs 가열 국면)가 반대인가
- 12행(아래) 붉은 트레이: 챔버 내 수직 위치/선반 정보와 대응하는가
- 줄무늬 트레이: 온도로 설명 안 되면 → 포메이션 채널 가설(D1)로 넘김 (온도 데이터 범위 밖)

## 5. 단계 5 — 검증 및 반증 시도

1. **홀드아웃 검증**: 트레이 80%로 회귀 적합 → 나머지 20% 트레이의 ΔdOCV7 필드를
   온도만으로 예측, 공간 상관 확인
2. **부호 일관성**: 예측 부호(링/역링)가 트레이별로 맞는가 — 진폭보다 부호가 강한 증거
3. **자리바꿈 검정(permutation)**: 트레이-온도이력 매칭을 셔플했을 때 상관이 사라지는가
   (스퓨리어스 공간 자기상관 배제)
4. **대안 가설 반증**: 온도로 설명되지 않는 잔차 필드에 여전히 구조(줄무늬 등)가
   남는지 확인 → 남으면 채널 매핑(D1)·스캔 순서(C4) 등 비온도 인자 몫으로 정량화

## 6. 단계 6 — 산출물

1. `report_temperature_tracing.md` — 결론 리포트:
   - 스텝 × 타깃 상관 히트맵 ("어느 공정에서 만들어져 어떻게 이어지는가" 한 장 요약)
   - 경로 P1/P2/P3 기여율 표
   - 패턴 유형별 원인 판정표
   - 공정 개선 권고 (예: 특정 스텝의 안정화 시간, 챔버 균일화, 판정 로직 보정)
2. 재현 가능한 분석 스크립트 (`analysis/` 디렉토리, Python)
3. 그림: 스텝별 ΔT 필드 갤러리 (기존 [27b] ΔOCV 갤러리와 나란히 비교)

---

## 7. 진행 순서 요약 및 선결 조건

| 순서 | 작업 | 선결 조건 |
|---|---|---|
| 1 | 온도 데이터 업로드 + 스키마 파악 | **데이터 업로드 필요** ← 다음 액션 |
| 2 | 셀ID-트레이좌표-공정스텝 조인, 필드 재구성 | OCV 데이터(전용OCV1~3)도 셀 단위로 필요 |
| 3 | 스텝별 구배 발생 지점 추적 | 2 완료 |
| 4 | P1/P2/P3 경로 판별 회귀 + 전파 사슬 분석 | 3 완료 |
| 5 | 홀드아웃/부호/permutation 검증 | 4 완료 |
| 6 | 리포트 작성·커밋 | 5 완료 |

### 데이터 요청 사항 (업로드 시 함께 확인)

- 온도 데이터: 공정 스텝별, 가능한 한 셀/트레이 좌표 수준, 타임스탬프 포함
- 전용OCV1~3 (또는 dOCV7) 셀 단위 데이터: 셀 ID + 트레이 ID + 트레이 내 좌표
- (있으면) 챔버 내 트레이 적재 위치/선반 정보, 포메이션 채널 ↔ 트레이 좌표 매핑
